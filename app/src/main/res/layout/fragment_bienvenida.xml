<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bienvenidaRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".MainActivity"
    android:padding="16dp">


    <!-- Título principal -->
    <TextView
        android:id="@+id/tvFutbolParaTodos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="30dp"
        android:text="Futbol Para Todos"
        android:textSize="36sp"
        android:textStyle="bold" />


    <!-- Bienvenida -->
    <TextView
        android:text="Bienvenido a Futbol Para Todos! En esta app encontrarás toda la información acerca de las grandes ligas y competiciones de Europa! Si sos nuevo te invitamos a registrarte!"
        android:textSize="18sp"
        android:layout_marginBottom="24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="16dp"
        android:paddingHorizontal="16dp">


        <!-- Botón Registrar -->
        <Button
            android:id="@+id/btnRegistro"
            android:text="Registrarme"
            android:textSize="20sp"
            android:backgroundTint="@android:color/black"
            android:textColor="@android:color/white"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"/>


        <!-- Botón Iniciar Sesión -->
        <Button
            android:id="@+id/btnIniciar"
            android:text="Iniciar sesión"
            android:textSize="20sp"
            android:backgroundTint="@android:color/black"
            android:textColor="@android:color/white"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"/>
    </LinearLayout>


</LinearLayout>

